#!/usr/bin/env sh

echo "🔍 커밋 메시지 규칙 검사 중..."

if ! npx --no -- commitlint --edit "$1"; then
  echo ""
  echo "커밋 메시지가 규칙에 맞지 않습니다."
  echo ""
  echo "제목 예시:"
  echo " feat: 로그인 기능 추가"
  echo ""
  echo "본문 예시:"
  echo " - input 상태 관리 추가"
  echo " - 유효성 검사 로직 개선"
  echo ""
  exit 1
fi

echo "커밋 메시지 규칙 통과"
